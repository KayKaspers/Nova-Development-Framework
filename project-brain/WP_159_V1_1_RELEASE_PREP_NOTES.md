# WP-159 — v1.1 Release Prep (Notes)

## Work Package

`NDF-WP-159 – v1.1 Release Prep` — bounded, docs-only release-preparation work package. Prepares the complete human-reviewable release package for a possible NDF v1.1.0 release; does not tag, release, push, or declare v1.1.0 released. Profile: Release Prep / Evidence Synthesis / Governance, Prompt Mode Full, budget B2 target / B3 maximum, B4 not authorised. Support skills: `ndf-work-package-runner` (routing), `ndf-release-safety` (version-neutral release governance), `ndf-release-notes-runner` (release notes structure and checks), `ndf-changelog-writer` (CHANGELOG entry, since `CHANGELOG.md` is in authorised scope).

## Baseline

Revision `e37f2c783732cef9feff2f31145e915f23a9bdde`, branch `main`, `origin/main` at the same commit, working tree clean, index empty, no in-progress merge/rebase/cherry-pick/revert/bisect (verified read-only). This is WP-158's own acceptance commit. v1.0.0 remains final released; the v1.x promise (ADR-0031) has been active since v1.0.0. v1.1 is planning-completed-through-WP-158, not scope-locked, not released.

## Target Version

`v1.1.0` — **not released, not tagged.**

## Result

**GO_WITH_NOTES** — the v1.1.0 release package is complete. Nova review has passed; Human-Maintainer acceptance of this WP-159 package is effective through the commit that carries this record. This is release-prep readiness only, not release approval: `WP_159_ACCEPTED != RELEASE_APPROVED != TAG_CREATED != RELEASE_PUBLISHED`. A separate, later, explicit Human-Maintainer release decision is still required (see `docs/release/V1_1_0_GO_NO_GO.md`, decision `PENDING_HUMAN_DECISION`). Full record: [V1_1_RELEASE_PREP_REVIEW.md](../docs/validation/v1-1/V1_1_RELEASE_PREP_REVIEW.md).

## Artifacts Created

- `docs/release/V1_1_0_RELEASE_NOTES.md`
- `docs/release/V1_1_0_GO_NO_GO.md`
- `docs/release/V1_1_0_TAGGING_AND_RELEASE_GUIDE.md` (reference only, not authorised for execution)
- `docs/release/V1_1_0_RELEASE_EVIDENCE_INDEX.md`
- `docs/validation/v1-1/V1_1_RELEASE_PREP_REVIEW.md`
- `project-brain/WP_159_V1_1_RELEASE_PREP_NOTES.md` (this file)

## Artifacts Modified

`CHANGELOG.md` (one new `[Unreleased]` entry for WP-159); `README.md` (current-state section, both DE/EN, WP-158 completed at `e37f2c7`, WP-159 release package prepared, v1.1.0 not released); `docs/roadmap/V1_1_PLAN.md` (WP-158 marked completed at `e37f2c7`, WP-159 marked as the current, final release-prep work package); `project-brain/CONTEXT_PACK_FOUNDATION_0_9.md` (Next Work Package / Compact Context History updated with WP-159); `project-brain/NEXT_PHASE_FOUNDATION_0_9.md` (Next WP paragraph extended with WP-159).

## Compatibility Conclusion

v1.x compatible. The release package itself introduces no breaking change — six new documentation artifacts and minimal current-state edits, no skill/ADR/prompt/adapter-convention change. No deprecation issued. No new ADR required; ADR-0032's private-consumer-skill-use question remains deliberately deferred (candidate ADR-0033).

## Known Limitations

Carried forward from WP-158, none resolved and none newly introduced by this work package:

- **G-13** — `KNOWN_LIMITATION` (depth addressed by WP-155; externality/independence open; closure needs a genuinely independent run).
- **PEV-004** — `KNOWN_LIMITATION` (thin project-enablement feedback evidence, honestly disclosed).
- **Single-maintainer bottleneck** — `STRUCTURAL` (not resolvable by any docs-only WP).
- **ADR-0032 private-consumer-skill scope** — `DEFERRED` (candidate ADR-0033; not opened by this WP).
- **Local neutrality denylist absent** — `STRUCTURAL` (secret, never repo-stored; CI authoritative).
- **Mixed i18n status** — `NON_BLOCKING` (tracked in `TRANSLATION_STATUS.md`).
- **Stale six-skill integrity records** — `CONSUMER_ACTION` (consumer-side refresh needed; not an NDF-side gap).
- **Foundation-0.9 Context Pack field drift** — `NON_BLOCKING` (tracked since WP-153/154).

## Blockers

None. No REWORK or BLOCKED condition was found in preparing this release package.

## Human-Maintainer Release Boundary

The Human Maintainer alone decides whether, when, and how v1.1.0 is released, and alone performs any staging, commit, push, tag, or GitHub release action. This work package performed no such action: no stage, no commit, no push, no fetch, no tag, no release, no GitHub write action, no network access, no package installation. The tagging/release guide created by this WP is explicitly reference-only; the actual executable release command sequence will be generated by Nova after this WP's review and Human-Maintainer acceptance.

## Final Release Decision Still Pending

`v1.1.0` is **prepared for Human-Maintainer release decision**, not released, not tagged, not published, not the current or latest release. `v1.0.0` remains the only final released version. Acceptance of this WP-159 package by commit is a distinct act from a release decision: `WP_159_ACCEPTED != RELEASE_APPROVED != TAG_CREATED != RELEASE_PUBLISHED`. The Go/No-Go artifact's final Human decision field stays `PENDING_HUMAN_DECISION` until the Human Maintainer makes a separate, later, explicit release decision.

## Compact Context Summary

WP-159 (docs-only, release-prep) prepared the complete human-reviewable v1.1.0 release package at baseline `e37f2c7` (WP-158's own acceptance commit; clean tree, empty index, no in-progress git operation, `origin/main` equal to `HEAD`). It synthesized the evidence from WP-151…158 — the seven WP notes files, the v1.1 validation records (WP-152…158), ADR-0031/0032, README/V1_1_PLAN/CHANGELOG/Context-Pack/Next-Phase, the skills README's Pack Model section, `TRANSLATION_STATUS.md`, and a freshly run Public Quality Gate (`--self-test` and `--strict`, both passed: 0 errors/0 warnings/3 notices, local denylist not configured — a pre-existing structural condition) — into six new durable artifacts: release notes (status "PREPARED — NOT RELEASED"), a Go/No-Go checklist (Human decision left `PENDING_HUMAN_DECISION`), a tagging/release guide explicitly labeled "REFERENCE ONLY — NOT AUTHORISED FOR EXECUTION", a release evidence index mapping every major claim to an existing source file, this WP's own release-prep review, and this notes file. No claim of release, publication, tagging, or approval was made anywhere in the package; G-13 is stated as `MATERIALLY_REDUCED — not closed` (not closed, not independently validated); ADR-0032's private-consumer-skill-use scope stays deferred to candidate ADR-0033; all eight known limitations WP-158 disclosed are carried forward unchanged and unresolved. Current-state documents (`CHANGELOG.md`, `README.md`, `docs/roadmap/V1_1_PLAN.md`, the Context Pack, and the Next-Phase file) were updated minimally to record WP-158 as completed/committed at `e37f2c7` and WP-159 as the current, final v1.1 release-prep work package with v1.1.0 "prepared for Human-Maintainer release decision" — never "released," "tagged," or "final." No ADR, Skill, or framework file was changed; no stage/commit/push/fetch/tag/release action was performed; no network access or package installation occurred. **Result: `GO_WITH_NOTES`** — the release package is complete; Nova review has passed and Human-Maintainer acceptance of this WP-159 package is effective through the commit carrying this record; this is release-prep readiness only, never a release decision (`WP_159_ACCEPTED != RELEASE_APPROVED != TAG_CREATED != RELEASE_PUBLISHED`). **Next authorised action: Human-Maintainer staging/commit/push of this complete WP-159 package. That commit alone does not authorise any tag or release action — a separate, later, explicit Human-Maintainer release decision (recorded in `docs/release/V1_1_0_GO_NO_GO.md`, currently `PENDING_HUMAN_DECISION`) is required before any executable tag/release contract may be issued, guided by the reference-only tagging guide and Nova's post-acceptance command sequence.**
